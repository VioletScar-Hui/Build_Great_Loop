# SHAKE — supervised promotion test

Before L3, run representative increments under supervision. Exercise every stop
path and safety denial. If recovery is promised, interrupt once after claim and
once after effect, then verify convergence. Record the exact harness/state hashes.

Acceptance: all seeded failures are detected; promotion evidence names the tested
hashes. Otherwise remain at the current autonomy level.
