# loop-spec 回归 — iteration-3（v2.2，2026-07-17）

改动背景：修复正文 `$数字` 被斜杠命令位置参数替换的 bug（见 CHANGELOG 2.2）。
行为面未改，跑 core-1 + gotcha-2 验证无退步。

## core-1（一句话想要个 loop）— PASS 4/4

跑法：子代理读 SKILL.md 后模拟首轮响应（假定无 loop-profile、用户在场）。

| 断言 | 判定 | 证据 |
|---|---|---|
| a1 先复述目标并请确认 | ✅ | "你要的是——…对吗？" + 范围外声明 + 「推断」标记 |
| a2 ≤4 题/轮、带具体选项 | ✅ | 3 题，各带选项与 ✦ 推荐 |
| a3 含用户没想到的选项 | ✅ | Q1-C 持续跑形态（运营层）；Q3-B 盲点扫描提议 |
| a4 确认前不产出 SPEC/提示词 | ✅ | 仅访谈；结尾按规范给出"接下来的流程"一行 |

修复直接验证：响应中 lite 判据渲染为"预算 0.5 美元以内"——正是原 bug
损坏的数字，此次以文字形式正确展示。

## gotcha-2（确定性检查）— PASS

`grep -n '\$[0-9]' SKILL.md` 零命中（exit 1）。
注：第一版维护注释本身含 `$0.5`/`$3` 字面量（自复发），已重写为
"美元符号+数字"描述式表述后通过。

## 未跑

core-2/3/4、edge-1/2、gotcha-1：本次改动不触及其行为面（数字格式 +
注释 + frontmatter version），判定低风险跳过。下次行为性改动应全量跑。
